apiVersion: v1
kind: PersistentVolume
metadata:
  name: mqtt-pv
spec:
  capacity:
    storage: 2Gi
  accessModes:
    - ReadWriteMany
  nfs:
    server: <IP_Servidor>
    path: /var/nfs/kubernetes-workshop/mqtt
